apply from: "../common_component_build.gradle"

android {
    resourcePrefix "service_" //给 Module 内的资源名增加前缀, 避免资源名冲突
}

dependencies {
    api project(':lib_select_photo')
    api rootProject.ext.dependencies["PhotoView"]
}